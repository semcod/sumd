Ticket STARTER-020 has been stuck in status 'waiting_input' for 28 cycles. Continue the actual implementation work for this ticket now. Run the relevant checks before closing it. Planfile status handoff:
- When checks pass and the work is complete, run: `planfile ticket done STARTER-020`
- If blocked or missing input, run: `planfile ticket input STARTER-020 --prompt "<exact input needed>" --note "<what you verified>"`
- If implementation fails after a real attempt, run: `planfile ticket fail STARTER-020 --error "<short failure reason>"`
- Do not leave completed IDE work in waiting_input.  Original ticket prompt: `project/analysis.toon.yaml` flags `cli` in LAYERS as a large/hot module (1212 lines, 0 classes, 29 methods, CC=14). Extract cohesive submodules around stable responsibilities and add focused regression tests before broad edits.
