Human-only GUI step (cannot be done headless): (1) Focus the running Qoder IDE (pid 46098) and open its AI chat panel. (2) Place the mouse pointer exactly over the chat text-input field and hold still. (3) In a terminal run: koru autopilot calibrate --ide qoder   (captures pointer pos after 5s, writes the 'qoder' entry to ~/.koru/ide-os-injector.json). (4) Verify: koru autopilot route --ide qoder  — the 'no chat calibration for qoder' line should clear. Follow-on (separate from calibration): on this GNOME/Wayland session the typing route is non-viable until you either export KORU_ALLOW_BLIND_KEYBOARD_FALLBACK=1 (ydotool_blind_injection) or connect the qoder IDE plugin.

Planfile status handoff:
- When checks pass and the work is complete, run: `planfile ticket done STARTER-019`
- If blocked or missing input, run: `planfile ticket input STARTER-019 --prompt "<exact input needed>" --note "<what you verified>"`
- If implementation fails after a real attempt, run: `planfile ticket fail STARTER-019 --error "<short failure reason>"`
- Do not leave completed IDE work in waiting_input.
