HUMAN-ONLY GUI step, cannot be done headless. (1) Focus the running Qoder window and open its AI chat panel. (2) Hover the mouse pointer exactly over the chat text-input field and HOLD STILL. (3) Run: koru autopilot calibrate --ide qoder  — it captures the live pointer position after a 5s delay and writes the qoder entry to ~/.koru/ide-os-injector.json, so the pointer MUST be on the chat field or the entry is bogus. (4) Verify: koru autopilot route --ide qoder  — the vdisplay_verified_injection line (currently: no chat calibration for qoder) must flip from X to OK. Only that one line is in scope; the typing/injection routes stay X on this GNOME/Wayland session (needs KORU_ALLOW_BLIND_KEYBOARD_FALLBACK=1 or the qoder IDE plugin) — separate follow-on, do not block on it.

Planfile status handoff:
- When checks pass and the work is complete, run: `planfile ticket done STARTER-019`
- If blocked or missing input, run: `planfile ticket input STARTER-019 --prompt "<exact input needed>" --note "<what you verified>"`
- If implementation fails after a real attempt, run: `planfile ticket fail STARTER-019 --error "<short failure reason>"`
- Do not leave completed IDE work in waiting_input.
