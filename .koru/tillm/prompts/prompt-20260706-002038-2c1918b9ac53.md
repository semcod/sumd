Ticket STARTER-019 has been stuck in status 'waiting_input' for 3 cycles. Continue the actual implementation work for this ticket now. Run the relevant checks before closing it. Planfile status handoff:
- When checks pass and the work is complete, run: `planfile ticket done STARTER-019`
- If blocked or missing input, run: `planfile ticket input STARTER-019 --prompt "<exact input needed>" --note "<what you verified>"`
- If implementation fails after a real attempt, run: `planfile ticket fail STARTER-019 --error "<short failure reason>"`
- Do not leave completed IDE work in waiting_input.  Original ticket prompt: Human-only GUI calibration for qoder (cannot run headless: calibrate captures live pointer coords after a 5s delay and would write a bogus qoder entry). Steps: (1) Focus the running Qoder IDE (pid 46098, verified alive) and open its AI chat panel. (2) Hover the mouse exactly over the chat text-input field and hold still. (3) In a terminal run: koru autopilot calibrate --ide qoder  (writes the qoder entry to ~/.koru/ide-os-injector.json). (4) Verify: koru autopilot route --ide qoder  — the no-chat-calibration-for-qoder line under vdisplay_verified_injection must clear. Follow-on (typing route, separate from calibration): still non-viable on this GNOME/Wayland session until you export KORU_ALLOW_BLIND_KEYBOARD_FALLBACK=1 OR connect the qoder IDE plugin.
